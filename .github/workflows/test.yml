name: Tests

on:
  push:
    branches: [ main, develop, pre-release ]
  pull_request:
    branches: [ main, develop, pre-release ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.11', '3.12']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov pytest-mock httpx

    - name: Run functional tests
      run: |
        pytest -q --no-cov

    - name: Run core coverage gate
      run: |
        pytest tests/ -q -o addopts='-v --strict-markers --tb=short --cov=src/analysis --cov=src/data --cov=src/reporting --cov=src/visualization --cov=src/core --cov-report=term-missing --cov-report=xml --cov-fail-under=60'

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install linting tools
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black mypy

    - name: Run flake8
      run: |
        flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Check code formatting with black
      run: |
        black --check src/

  react-ui-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install dependencies
      working-directory: react_ui
      run: npm ci

    - name: Run type check
      working-directory: react_ui
      run: npm run type-check

    - name: Run tests
      working-directory: react_ui
      run: npm test -- --run
